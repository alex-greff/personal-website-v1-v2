const state = {
    currentTheme: "default"
};

const getters = {
    getCurrentTheme: state => {
        return state.currentTheme;
    }
};

const mutations =  {

};

const actions = {

};

export default {
    state,
    mutations,
    actions,
    getters
}
